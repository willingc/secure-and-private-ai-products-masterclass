FROM ubuntu:22.04

RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

WORKDIR /models

COPY .devcontainer/llamafile-init.sh /usr/local/bin/llamafile-init.sh
RUN chmod +x /usr/local/bin/llamafile-init.sh

EXPOSE 8080

ENTRYPOINT ["/usr/local/bin/llamafile-init.sh"]

